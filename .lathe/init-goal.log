Done. Here's what was written:

**`.lathe/goal.md`** — The customer champion's behavioral doc. Five stakeholders (Alex the self-hosting developer, Morgan the production operator, Jordan the contributor, Sam the security evaluator, Casey the contract consumer), each with a first-encounter journey, an emotional signal, what builds trust, and what makes them leave. Three tensions with concrete resolution signals. Ranking via two sources: CI floor first, then lived experience. The job as a 6-step loop. No layer ladder.

**`.lathe/skills/journeys.md`** — Concrete shell commands for each stakeholder's journey that the champion runs each cycle, plus what to watch for at each step.

**`.lathe/skills/testing.md`** — The full testing picture: CI gates (validate.yml, ha-gate.yml), local quality gates that match CI exactly, contract validator fixtures, Python unit tests, shellcheck, and the constitutional gates.

**`.lathe/skills/build.md`** — How the project builds (kind cluster, Helm chart development, the vendor image pipeline, ArgoCD manifests, the ceremony system).

**`.lathe/skills/architecture.md`** — Key decisions in force: core invariants, component map, contract system, sprint model, sovereignty tiers.

**`.lathe/skills/domains.md`** — Domain knowledge map across Kubernetes/k3s, Helm, ArgoCD, Istio, Rook/Ceph, Crossplane, OpenBao, OPA/Gatekeeper, the observability stack, Cloudflare, cloud providers, and shell/Bash — including a boundary confusion guide for the cases where a symptom points to the wrong domain.

**`.lathe/alignment-summary.md`** — For you. Stakeholder one-liners, emotional signals, tension resolution signals, and what could be wrong (unverified branch protection, Istio STRICT vs. PERMISSIVE default, Forgejo vs. GitHub CI question).
