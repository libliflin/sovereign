Clean output, 1,550 chars — well within the 6,000-char budget. All sections pass green. Here's what the script covers:

- **Git status + recent commits** — cycle-to-cycle change signals
- **Helm lint** — 34 charts, summarized to one line on green
- **Autarky gate (G6)** — no external registry refs in templates
- **Contract validator (G7)** — valid accepted, invalid rejected
- **Shellcheck** — all `.sh` files in `cluster/`, `platform/`, `scripts/`
- **sovereign-pm typecheck + lint + tests** — 26 passed, one-liners on green, expands on failure
- **CI config** — lists workflow files
- **Sprint state** — active increment + story counts (done/passing/todo) from `prd/manifest.json`

Every section shows a single summary line on the happy path and expands with failure details only when something breaks.
