2,249 characters — well under the 4,000 target with plenty of headroom. The script is done.

`.lathe/snapshot.sh` is written and executable. Here's what it captures:

- **Git status + log** — what changed, recent history
- **Sprint state** — reads `prd/manifest.json` to find the active increment, reports pass/review/limbo counts with limbo story titles
- **Constitutional gates** — G1 (ceremony scripts compile + imports), G6 (no external registries in templates), G7 (contract validator enforces sovereignty)
- **Python tests** — runs `scripts/ralph/tests/test_*.py` directly (no pytest required), reports pass/fail counts
- **Shellcheck** — across `scripts/ralph`, `cluster/kind`, `platform`, `bootstrap`
- **Helm charts** — total count + lint check on recently-modified charts
- **CI workflows** — lists `.github/workflows/` files

Current run: all gates PASS, 3/3 tests pass, 24 scripts clean, 40 charts, 2,249 chars.
