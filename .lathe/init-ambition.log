Both files are written. Here's what was produced:

**`.lathe/ambition.md`** — full destination sheet:

- **Destination**: The self-hoster clones on Friday, runs `bootstrap.sh` on real VPS, and has every service URL passing `./bootstrap/verify.sh` by Sunday. Cited from README.md:1–3, scope.md:1, architecture.md:11, and CLAUDE.md T3.

- **4 concrete gaps**, all cited from real project signals:
  1. End-to-end VPS bootstrap unwalked — `platform/deploy.sh` is a confirmed scaffold (increment 34 notes), `config.yaml.example` was just added as a stub (commit `942b1d7`)
  2. Backstage developer portal has no Keycloak OIDC — 3 E11 stories still pending
  3. code-server extension autarky (DEVEX-015) unreviewed, agents still hit `marketplace.visualstudio.com`
  4. HA gate not uniformly green — `check-limits.py` only added in increment 40, chart corpus not re-validated

- **What on-ambition vs. off-ambition looks like** for each gap — gives the champion the patch-vs-structural test.

- **Velocity signal**: late assembly mode, not MVP mode — the "make the entry path visible" commit cluster (`942b1d7`, `cb97c37`, `91d5626`, `7393d1a`) is the tell.

**`alignment-summary.md`** — Ambition section appended with destination, gaps, and a clear "what could be wrong" note flagging that the destination comes from aspirational docs, not a verified end-to-end run.
