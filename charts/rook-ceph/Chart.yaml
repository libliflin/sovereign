apiVersion: v2
name: rook-ceph
description: Rook/Ceph distributed encrypted storage for Sovereign Platform — operator + cluster
type: application
version: 0.1.0
appVersion: "1.13.0"

dependencies:
  - name: rook-ceph
    version: "1.13.0"
    repository: "https://charts.rook.io/release"
    alias: operator
  - name: rook-ceph-cluster
    version: "1.13.0"
    repository: "https://charts.rook.io/release"
    alias: cluster
