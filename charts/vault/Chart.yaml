apiVersion: v2
name: vault
description: HashiCorp Vault for Sovereign Platform — dev mode by default, HA production mode togglable
type: application
version: 0.1.0
appVersion: "1.15.0"

dependencies:
  - name: vault
    version: "0.27.0"
    repository: "https://helm.releases.hashicorp.com"
    alias: vault
