apiVersion: v2
name: cert-manager
description: cert-manager with self-signed and Vault issuers for Sovereign Platform
type: application
version: 0.1.0
appVersion: "1.14.0"

dependencies:
  - name: cert-manager
    version: "1.14.0"
    repository: "https://charts.jetstack.io"
    alias: cert-manager
