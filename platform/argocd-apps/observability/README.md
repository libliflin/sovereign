# ArgoCD Applications — Observability

This directory contains ArgoCD Application manifests for the **observability** tier.

Observability apps: kube-prometheus-stack (Prometheus + Grafana + AlertManager), Loki (logs), Thanos (long-term metrics), Tempo (traces).
