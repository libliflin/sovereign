apiVersion: v2
name: cilium
description: Cilium CNI with Hubble observability, WireGuard encryption, and deny-all network policies for the Sovereign Platform
type: application
version: 0.1.0
appVersion: "1.16.0"

dependencies:
  - name: cilium
    version: "1.16.0"
    repository: "https://helm.cilium.io/"
    alias: cilium
