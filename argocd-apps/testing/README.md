# ArgoCD Applications — Testing Infrastructure

This directory contains ArgoCD Application manifests for the **testing** tier.

Testing apps: Selenium Grid (browser testing), k6 Operator (load testing), Wiremock (API mocking), MailHog (email testing), Chaos Mesh (resilience testing).
