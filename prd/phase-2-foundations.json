{
  "phase": 2,
  "name": "foundations",
  "description": "Front door security (Cloudflare tunnel, VPS hardening), HA multi-node cluster provisioning with kube-vip and etcd quorum enforcement.",
  "sprintGoal": "Every new cluster is provisioned as a 3+ node HA system with all inbound ports closed behind a Cloudflare tunnel \u2014 zero manual firewall configuration required.",
  "capacity": 15,
  "stories": [
    {
      "id": "012",
      "phase": 2,
      "priority": 1,
      "branchName": "feature/bootstrap-frontdoor-interface",
      "title": "Bootstrap: front door provider interface and VPS hardening framework",
      "description": "Create a pluggable 'front door' security provider pattern for the bootstrap. Add bootstrap/frontdoor/interface.sh defining 5 shell function hooks every provider must implement: frontdoor_provision (local: create tunnel/service), frontdoor_install_agent (remote: install daemon on VPS), frontdoor_allowed_cidrs (returns newline-separated CIDRs to whitelist \u2014 everything else dropped), frontdoor_configure_dns (point DNS records), frontdoor_connection_info (print how to connect after bootstrap). Add bootstrap/frontdoor/none.sh as the baseline impl (allowed_cidrs prompts for caller IP, no agent, prints SSH instructions). Add hardening scripts that ALWAYS run regardless of front door: bootstrap/hardening/base.sh (unattended-upgrades, fail2ban, auditd, purge unused packages), bootstrap/hardening/ssh.sh (PasswordAuthentication no, PermitRootLogin prohibit-password, PubkeyAuthentication yes), bootstrap/hardening/kernel.sh (CIS benchmark sysctl: tcp_syncookies, rp_filter, dmesg_restrict, suid_dumpable=0), bootstrap/hardening/firewall.sh (UFW default-deny, reads frontdoor_allowed_cidrs output, opens only those CIDRs). Update bootstrap/config.yaml.example to add frontDoor field (default: cloudflare). Update bootstrap/bootstrap.sh to source the chosen front door impl and call hooks in order: harden \u2192 frontdoor_provision \u2192 frontdoor_install_agent (remote) \u2192 frontdoor_configure_dns \u2192 firewall.sh \u2192 frontdoor_connection_info.",
      "acceptanceCriteria": [
        "bootstrap/frontdoor/interface.sh documents all 5 required hooks with usage comments",
        "bootstrap/frontdoor/none.sh implements all 5 hooks (allowed_cidrs prompts for caller IP)",
        "bootstrap/hardening/base.sh installs and enables unattended-upgrades, fail2ban, auditd",
        "bootstrap/hardening/ssh.sh disables password auth, root password login, enforces pubkey",
        "bootstrap/hardening/kernel.sh applies CIS benchmark sysctl settings",
        "bootstrap/hardening/firewall.sh calls frontdoor_allowed_cidrs and applies UFW rules",
        "bootstrap/bootstrap.sh sources frontdoor impl and calls hooks in correct order",
        "bootstrap/config.yaml.example has frontDoor field with cloudflare as default",
        "shellcheck passes on all new .sh files"
      ],
      "passes": true,
      "points": 3,
      "testPlan": "shellcheck bootstrap/frontdoor/*.sh bootstrap/hardening/*.sh bootstrap/bootstrap.sh; verify all 5 hook functions defined in interface.sh; verify none.sh implements all 5; verify hardening scripts are sourced in bootstrap.sh; verify config.yaml.example has frontDoor field.",
      "dependencies": [],
      "reviewed": false,
      "reviewNotes": [],
      "attempts": 1,
      "smart": {
        "specific": 5,
        "measurable": 5,
        "achievable": 4,
        "relevant": 5,
        "timeBound": 5,
        "notes": "All ACs are binary file/function/shellcheck checks. Well-scoped."
      }
    },
    {
      "id": "013",
      "phase": 2,
      "priority": 2,
      "branchName": "feature/bootstrap-frontdoor-cloudflare",
      "title": "Bootstrap: Cloudflare front door implementation",
      "description": "Implement bootstrap/frontdoor/cloudflare.sh with all 5 interface hooks using Cloudflare Tunnel + Zero Trust Access (free tier). frontdoor_provision: use Cloudflare API (token + account ID from config.yaml) to create a named tunnel, store tunnel credentials JSON, create a cloudflared config.yml for the VPS. frontdoor_install_agent: SSH to VPS, install cloudflared deb package, write /etc/cloudflared/config.yml, enable and start cloudflared.service as a systemd unit. frontdoor_allowed_cidrs: fetch https://www.cloudflare.com/ips-v4 and https://www.cloudflare.com/ips-v6 and echo them (no local ports needed \u2014 tunnel is outbound-only). frontdoor_configure_dns: use Cloudflare API to create CNAME records routing *.<domain> to the tunnel hostname. frontdoor_connection_info: print instructions for using 'cloudflare access ssh' and the Zero Trust dashboard URL. Add bootstrap/providers/cloudflare-dns.sh as a shared helper for Cloudflare API calls (zone lookup, record create/update) reusable by both the front door and any provider that needs DNS. config.yaml.example cloudflare section needs: apiToken, accountId, tunnelName, zoneId.",
      "acceptanceCriteria": [
        "bootstrap/frontdoor/cloudflare.sh implements all 5 hooks",
        "frontdoor_provision creates a Cloudflare Tunnel via API and writes tunnel credentials",
        "frontdoor_install_agent installs cloudflared on the VPS as a systemd service via SSH",
        "frontdoor_allowed_cidrs fetches live Cloudflare IP ranges (v4 + v6)",
        "frontdoor_configure_dns creates wildcard CNAME via Cloudflare API",
        "bootstrap/providers/cloudflare-dns.sh has reusable API helper functions",
        "config.yaml.example has cloudflare.apiToken, accountId, tunnelName, zoneId",
        "shellcheck passes on all new .sh files",
        "After running: VPS has no open inbound ports, all traffic flows through tunnel"
      ],
      "passes": true,
      "points": 2,
      "testPlan": "shellcheck bootstrap/frontdoor/cloudflare.sh bootstrap/providers/cloudflare-dns.sh; verify all 5 hook functions present; verify config.yaml.example has cloudflare section; verify frontdoor_allowed_cidrs output contains IP ranges.",
      "dependencies": [
        "012"
      ],
      "reviewed": false,
      "reviewNotes": [],
      "attempts": 1,
      "smart": {
        "specific": 5,
        "measurable": 5,
        "achievable": 4,
        "relevant": 5,
        "timeBound": 5,
        "notes": "Concrete API calls and file outputs. ACs are binary. Achievable 4: Cloudflare API dependency."
      }
    },
    {
      "id": "014",
      "phase": 2,
      "priority": 3,
      "branchName": "feature/bootstrap-frontdoor-docs",
      "title": "Bootstrap: front door provider docs and custom provider template",
      "description": "Add bootstrap/frontdoor/custom.sh.example as a heavily commented template for users implementing their own front door (WireGuard, Tailscale, custom reverse proxy, etc.) \u2014 each of the 5 hooks stubbed with comments describing exactly what it must do. Add docs/providers/cloudflare-setup.md covering: Cloudflare free account setup, creating an API token with Tunnel+DNS edit permissions, finding account ID and zone ID, Zero Trust SSH access setup, and how the bootstrap uses the tunnel. Add docs/providers/front-door-custom.md explaining the provider pattern, the 5 hooks contract, and a worked example implementing a WireGuard-based front door. Update docs/quickstart.md to include the front door selection step and security posture explanation (default: all ports closed, SSH via tunnel).",
      "acceptanceCriteria": [
        "bootstrap/frontdoor/custom.sh.example stubs all 5 hooks with detailed comments",
        "docs/providers/cloudflare-setup.md covers full Cloudflare account and tunnel setup",
        "docs/providers/front-door-custom.md explains the 5-hook contract with WireGuard example",
        "docs/quickstart.md updated with front door selection and security posture section",
        "All markdown files render without broken links or malformed headings"
      ],
      "passes": true,
      "points": 2,
      "testPlan": "Check all 5 hooks stubbed in custom.sh.example; verify docs exist; verify quickstart.md has frontDoor section.",
      "dependencies": [
        "012"
      ],
      "reviewed": false,
      "reviewNotes": [],
      "attempts": 1,
      "smart": {
        "specific": 5,
        "measurable": 5,
        "achievable": 5,
        "relevant": 4,
        "timeBound": 5,
        "notes": "Pure documentation and template files. All ACs are file-existence checks."
      }
    },
    {
      "id": "015",
      "phase": 2,
      "priority": 4,
      "branchName": "feature/bootstrap-ha-multi-node",
      "title": "Bootstrap HA: multi-node provisioning, kube-vip, and etcd quorum enforcement",
      "description": "Refactor all provider scripts and bootstrap.sh to enforce HA as a hard requirement. bootstrap.sh must validate nodes.count is odd and >= 3 \u2014 print a clear error and abort if not (single-node and even-node setups are not supported). Update bootstrap/config.yaml.example to add nodes section with count: 3 and serverType. Refactor all four provider scripts (hetzner.sh, generic-vps.sh, aws-ec2.sh, digitalocean.sh) to loop over node count and provision all N nodes. On each provider: after all nodes are running, install kube-vip on each control plane node to create a floating API server VIP. Install K3s using --cluster-init with embedded etcd on node 1 (pointing tls-san at the VIP). Join nodes 2+ using --server https://<VIP>:6443. Wait for all N nodes to be Ready before proceeding. Output kubeconfig pointing at the VIP, not a single node IP. Update bootstrap/verify.sh to check: node count >= 3, all nodes Ready, etcd cluster healthy (etcdctl endpoint health), kube-vip VIP is reachable. Update frontdoor_install_agent hook in bootstrap/frontdoor/interface.sh to accept a list of all node IPs (not just one) \u2014 cloudflared must be installed on every node so the tunnel remains up if a node fails. Update cloudflare.sh to install cloudflared on all nodes and create a DaemonSet manifest instead of a single systemd unit. Update all provider cost documentation to show 3-node costs, not single-node. Remove all references to AWS free tier (t2.micro is not viable \u2014 minimum is t3.small).",
      "acceptanceCriteria": [
        "bootstrap.sh aborts with clear error if nodes.count < 3 or even",
        "bootstrap/config.yaml.example has nodes.count: 3 and nodes.serverType",
        "hetzner.sh provisions nodes.count servers in a loop",
        "generic-vps.sh accepts comma-separated list of node IPs for multi-node setup",
        "aws-ec2.sh provisions nodes.count t3.small (minimum) instances",
        "digitalocean.sh provisions nodes.count droplets",
        "kube-vip installed on all control plane nodes before K3s joins",
        "K3s node 1 uses --cluster-init, nodes 2+ use --server https://<VIP>:6443",
        "bootstrap/verify.sh checks all nodes Ready, etcd healthy, VIP reachable",
        "frontdoor_install_agent receives all node IPs and installs cloudflared on each",
        "cloudflare.sh deploys cloudflared as a K3s manifest (DaemonSet) not just systemd",
        "Provider docs updated: all costs show 3-node totals, AWS free tier removed",
        "shellcheck passes on all modified scripts"
      ],
      "passes": true,
      "points": 3,
      "testPlan": "shellcheck bootstrap/**/*.sh; verify loop pattern in each provider; verify kube-vip install in each provider; verify cluster-init/server flags; verify verify.sh checks etcd; verify cloudflare.sh DaemonSet manifest.",
      "dependencies": [
        "012",
        "013"
      ],
      "reviewed": false,
      "reviewNotes": [],
      "attempts": 1,
      "smart": {
        "specific": 5,
        "measurable": 5,
        "achievable": 4,
        "relevant": 5,
        "timeBound": 5,
        "notes": "Complex multi-script refactor. All ACs are verifiable with grep/shellcheck. Achievable 4: touches many scripts."
      }
    }
  ]
}